// verification/stack-detect.ts — Tech stack auto-detection (stub)
// Populated with real implementation in TODO #4.

/** Tech stack type — expanded in TODO #4 */
export type TechStack = 'general';

/** Detect primary tech stack from project files */
export function detectStack(_cwd: string): TechStack {
  return 'general';
}

/** Get stack-specific verification hints */
export function getStackHint(_cwd: string): string {
  return '';
}
